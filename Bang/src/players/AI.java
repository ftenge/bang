package players;

public class AI extends Player {
    public AI(String name, Character character) {
        super(name, character);
    }

    public void makeDecision(Game game) {
        // AI logic to decide actions
    }
}